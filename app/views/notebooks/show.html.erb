<h2><%= @notebook.name %></h2>

<%= button_to 'New page', "#{@notebook.id}/pages" %>

<%= turbo_frame_tag 'pages' do %>
  <% if @notebook.pages.empty? %>
    <p>Nothing to show</p>
  <% else %>
    <div>
      <%= turbo_frame_tag 'pages' do %>
        <div>
          <div>Title</div>
          <div>Updated at</div>
          <div></div>
        </div>
        <div>
          <% @pages.each do |page| %>
            <div><%= link_to page.title, notebook_page_path(@notebook.id, page.id), data: { turbo_frame: '_top' } %></div>
            <div><%= page.updated_at.strftime("%d-%m-%Y") %></div>
            <div>Delete</div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% end %>
<% end %>